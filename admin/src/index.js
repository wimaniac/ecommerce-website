import React from "react";
import ReactDOM from "react-dom/client";  // Cập nhật đúng cho React 18+
import App from "./App";
import "./App.css";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


